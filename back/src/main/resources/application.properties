jwt.public.key=classpath:app.pub
jwt.private.key=classpath:app.key

spring.application.name=ifome_api
spring.profiles.active=${PROFILE:dev}
# TODO arrumar schema
#spring.sql.init.mode=always
#spring.jpa.defer-datasource-initialization=true

spring.datasource.url=jdbc:h2:mem:devdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.h2.console.enabled=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:noreply.ifome@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:gfjo meti rkmp xwet}

# Diretório onde as imagens serão salvas
file.upload-dir=C:/path/to/your/directory/uploads/ 

# URL base para acessar as imagens
file.base-url=http://localhost:8080/

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
